window.torro = window.torro || {};

( function( torro ) {
	'use strict';

	/**
	 * A single form.
	 *
	 * @class
	 * @augments torro.Builder.BaseModel
	 */
	torro.FormModel = torro.BaseModel.extend({

	});
})( window.torro );
